Create a powershell (7.4 and higher) module that implements the AWX/AAP rest api

The API is available at http://localhost:32000/api/v2

Create the public and private functions as separate files. 
Create a basic script to build the module into one psm1 and psd1 in the Output folder

Start simple, create functions for login and the 'me' api
From there I'll see what to do next
